{
  "name": "community",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently -k -n \"client,api\" -c \"bgBlue,bgMagenta\" \"npm:dev --prefix client\" \"npm:dev --prefix api\"",
    "dev:api": "npm dev --prefix api",
    "dev:client": "npm dev --prefix client",
    "prod": "concurrently -k -n \"client,api\" -c \"bgBlue,bgMagenta\" \"npm:serve --prefix client\" \"npm:start --prefix api\"",
    "prod:api": "npm serve --prefix api",
    "prod:client": "npm start --prefix client"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/reddign/community.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/reddign/community/issues"
  },
  "homepage": "https://github.com/reddign/community#readme",
  "devDependencies": {
    "concurrently": "^8.0.1",
    "lerna": "^6.6.1"
  },
  "dependencies": {
    "react-bootstrap-icons": "^1.10.3",
    "react-error-boundary": "^4.0.4"
  }
}
